import React from 'react';
import { Link } from 'react-router-dom';

const NotFound = () => {
    return (
        <main>
            <h1>NotFound</h1>
        </main>
    )
};

export default NotFound;