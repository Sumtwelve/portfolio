import React from 'react';
import { Link } from 'react-router-dom';

const NotFound = () => {
    return (
        <main>
            <h2>NotFound</h2>
        </main>
    )
};

export default NotFound;