import React from 'react';
import BackBtn from '../components/BackBtn';

const Resume = () => {
    return (
        <main>
            <h2>Resume</h2>
            <BackBtn />
        </main>
    )
};

export default Resume;